1、输入ip:8000直接跳转的注册页面
2、如果已经注册的话，则直接选择“已有账户，直接登录”例如：账号root  密码123，密码则是MD5加密
3、后面做了对密码的判断，只能满足小于16位，并且有数字和字母
4、目前没有设置管理账号，目前不管是后添加的账号还是自己注册的账号，都可以登录
5、登录后的页面只是采用了bootstrap的导航和栅格系统，但是栅格系统并没有因为页面缩小而变化，不知道怎么搞了
6、导航上的搜索框可以输入id号，用户名，手机号对以存在的用户进行搜索
7、右侧的消息提示没弄出来，用户信息那块不知道显示已经登录的用户
8、body的左侧目前只有4个功能，添加，删除，修改手机号和修改密码
9、右侧显示所用注册和创建的用户信息
10、下面有页码，每页只显示10条数据，页码也会随着数据的增加而增加
11、每个form表单都使用了csrf_token进行验证

